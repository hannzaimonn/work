<h1>Editing Book</h1>
<%= form_for(@blog) do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Body</h4>
    <%= f.text_area :body %>
    <%= f.submit 'Update Book' %>
    <td>
    <%= link_to "show", post_path(book.id) %>
    |<%= link_to 'Back', books_path %></td>
<% end %>